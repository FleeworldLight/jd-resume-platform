# Web 框架
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0

# 数据校验
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0

# 数据库（SQLite，本地零外部服务）
SQLAlchemy>=2.0.36,<3.0
alembic>=1.13,<2.0
aiosqlite>=0.20,<1.0

# LLM 编排（LangChain）
langchain>=0.3,<1.0
langchain-core>=0.3,<1.0
langchain-openai>=0.2,<1.0
langchain-anthropic>=0.3,<1.0

# HTTP 客户端
httpx>=0.27,<1.0

# PDF 导出
reportlab>=4.2,<5.0

# 爬虫（默认启用，需 playwright browsers）
playwright>=1.48,<2.0

# 简历解析
pypdf>=5.0,<6.0
python-docx>=1.1,<2.0

# 数值计算
numpy>=2.1,<3.0

# 加密
cryptography>=43,<44

# 文件上传
python-multipart>=0.0.20,<1.0

# 测试
pytest>=8.3,<9.0
pytest-asyncio>=0.24,<1.0
pytest-cov>=6,<7.0