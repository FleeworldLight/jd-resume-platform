# jd-resume-platform 项目梳理报告

> 生成时间：2026-09-11
> 性质：只读审计。**本次未改动、未移动、未删除任何文件**，以下全部是待你执行的建议。
> 你的决策：爬面经 → 移出仓库；Docker/Railway 残留 → 直接删除；其余部分本报告给出方案。

---

## 0. 一句话结论

平台本体（`backend/` + `frontend/` + `start.bat`）是健康的，双击就能跑。乱的部分全在**仓库外壳**：

1. 上一阶段（Postgres/Redis/Celery/Docker）的部署残留没清
2. 一份已经失效的交接文档 `NEXT_AI_TODO.md` 还留着，会误导后续开发
3. 一个完全无关的项目 `爬面经/` 混在仓库根目录
4. 一轮"极简化改造"改了几十个文件，**一个 commit 都没提交**

---

## 1. 现状体检表

### 1.1 根目录

| 条目 | 判定 | 处置 |
|---|---|---|
| `backend/` | 核心资产 | 保留 |
| `frontend/` | 核心资产 | 保留 |
| `start.bat` | 核心资产 | 保留（提示文案需修正，见 §4.4） |
| `README.md` | 核心资产 | 保留（少数条目需修正，见 §4.4） |
| `DEVELOPMENT_LOG.md` | 有效 | 保留，追加一条本轮记录 |
| `.gitignore` | 有效 | 保留，补 `*.tsbuildinfo` |
| `.env.example` | **冗余** | **删除**（应用实际不读它，已核实，见 §4.3） |
| `data/` | **空壳** | **删除**（只剩 `resumes/.gitkeep`，见 §4.4） |
| `scripts/` | **过时** | **删除**（内容为 postgres 初始化） |
| `docs/` | 混合 | 逐篇复核，见 §4.2 |
| `NEXT_AI_TODO.md` | **失效** | **删除**，见 §4.1 |
| `DEPLOY-RAILWAY.md` | **过时** | **删除** |
| `docker-compose.yml` | **过时** | **删除** |
| `docker-compose.dev.yml` | **过时** | **删除** |
| `爬面经/` | **外来项目** | **移出仓库** |

### 1.2 backend/

| 条目 | 判定 | 处置 |
|---|---|---|
| `app/`（约 50 个 .py，分层清晰） | 核心资产 | 保留 |
| `tests/`（11 个文件） | 核心资产 | 保留 |
| `alembic/`（仅 `0001_init.py`） | 核心资产 | 保留 |
| `requirements.txt`（已极简化） | 核心资产 | 保留 |
| `.env.example` | **有效** | **保留**（这才是真正被加载的那份） |
| `Dockerfile` | **过时** | **删除** |
| `data/`（含 `jd_platform.db`） | 运行时数据 | 保留（真正的数据库在这里） |
| `.venv/`、`.pytest_cache/` | 已被 ignore | 保留 |

### 1.3 frontend/

| 条目 | 判定 | 处置 |
|---|---|---|
| `src/`（12 个文件，5 个页面） | 核心资产 | 保留 |
| `package.json` / `vite.config.ts` / `tailwind.config.js` / `tsconfig.json` / `postcss.config.js` / `index.html` | 核心资产 | 保留 |
| `Dockerfile` | **过时** | **删除** |
| `nginx.conf` | **过时** | **删除** |
| `node_modules/`、`dist/`、`tsconfig.tsbuildinfo` | 构建产物 | 保留（前两项已 ignore，第三项需补规则） |

---

## 2. 五类问题详述

### 2.1 一整个无关项目混进来了：`爬面经/`

内容：`xiaolincoding_crawler.py`（小林coding 面经爬虫）+ `md/` 56 篇 + `pages/` 57 个 HTML + `manifest.json` + `index.html`（本地离线浏览站点）。

它与简历平台**没有任何代码或业务关系**，且完全未被 git 跟踪。这是根目录"看起来乱"的最大单一来源。

**处置：移出到同级目录**，作为独立项目存在。

### 2.2 过时的部署残留

这些文件全部来自"Docker Compose + Postgres + Redis + Celery"阶段，与当前"SQLite + 双击启动"方案直接冲突：

- `docker-compose.yml` — 定义 5 个服务（postgres / redis / backend / celery-worker / frontend）
- `docker-compose.dev.yml` — pgvector + redis 依赖
- `DEPLOY-RAILWAY.md` — Railway 部署说明，依赖上面那套
- `scripts/init-postgres.sql` — 创建 `vector` / `zhparser` 扩展
- `backend/Dockerfile` — 内含 weasyprint 所需 Pango/Cairo 系统依赖注释
- `frontend/Dockerfile` + `frontend/nginx.conf` — 静态站点容器化

### 2.3 文档与现状脱节

- `NEXT_AI_TODO.md` 声称的任务**已经全部完成**（详见 §4.1），却仍在作为"交接文档"存在 —— 危害最大的一项
- `docs/` 13 篇中 10 篇仍提到 Docker / Celery / Postgres / Redis，其中：
  - `docs/deployment/local.md` 开篇即写"用 Docker Compose 一键启动全部依赖 + 应用"，与 README 完全矛盾
  - `docs/architecture/async-tasks.md` 整篇讲 Celery 异步任务，而 `app/tasks/` 目录已被删除
  - `docs/testing/strategy.md` 要求 Poetry / pnpm，实际使用 venv / npm

### 2.4 重复文件

- 根 `.env.example` 与 `backend/.env.example` **内容逐字相同**
- `docker-compose.yml` 与 `docker-compose.dev.yml` 定位重叠

### 2.5 配置与路径口径不一致

- README 和 `start.bat` 都提示数据库在 `./data/jd_platform.db`
- 实际数据库在 `backend/data/jd_platform.db`
- 根目录 `data/` 目录基本是空壳

（详见 §4.3、§4.4）

---

## 3. 执行方案

### 步骤 1 — 移出 `爬面经/`

```bash
cd "G:/期末及简历和别的项目/测试"
mv jd-resume-platform/爬面经 ./爬面经
```

结果：`G:\期末及简历和别的项目\测试\爬面经\` 成为独立项目目录，本仓库不再包含它。

> 若希望它也能享受版本管理，可考虑在该目录 `git init` 单独成库。

### 步骤 2 — 删除过时部署文件

> **⚠️ 此操作非常危险，可能导致不可逆的数据丢失！**
> 请先阅读下表"可恢复性"一列，再决定逐条执行还是全部执行。

| 完整路径 | git 状态 | 可恢复性 |
|---|---|---|
| `G:\期末及简历和别的项目\测试\jd-resume-platform\docker-compose.yml` | 已跟踪 | ✅ 可从 git 历史找回 |
| `G:\期末及简历和别的项目\测试\jd-resume-platform\docker-compose.dev.yml` | 已跟踪 | ✅ 可从 git 历史找回 |
| `G:\期末及简历和别的项目\测试\jd-resume-platform\scripts\init-postgres.sql` | 已跟踪 | ✅ 可从 git 历史找回 |
| `G:\期末及简历和别的项目\测试\jd-resume-platform\backend\Dockerfile` | 已跟踪 | ✅ 可从 git 历史找回 |
| `G:\期末及简历和别的项目\测试\jd-resume-platform\frontend\Dockerfile` | 已跟踪 | ✅ 可从 git 历史找回 |
| `G:\期末及简历和别的项目\测试\jd-resume-platform\frontend\nginx.conf` | 已跟踪 | ✅ 可从 git 历史找回 |
| `G:\期末及简历和别的项目\测试\jd-resume-platform\DEPLOY-RAILWAY.md` | **未跟踪** | ❌ 删除后无法找回 |
| `G:\期末及简历和别的项目\测试\jd-resume-platform\NEXT_AI_TODO.md` | **未跟踪** | ❌ 删除后无法找回 |

删除后 `scripts/` 目录将变空，可一并移除。

执行方式（推荐用 `git rm`，前 6 项可留痕于历史）：

```bash
cd "G:/期末及简历和别的项目/测试/jd-resume-platform"
git rm docker-compose.yml docker-compose.dev.yml scripts/init-postgres.sql \
       backend/Dockerfile frontend/Dockerfile frontend/nginx.conf
rmdir scripts 2>/dev/null
rm DEPLOY-RAILWAY.md NEXT_AI_TODO.md
```

**关于后两个未跟踪文件**：如不希望彻底失去内容，建议删除前先把关键信息誊抄进 `DEVELOPMENT_LOG.md`（尤其 `NEXT_AI_TODO.md` 里的"关键点提醒"三条：LangChain 可去可留、customize 接口改同步后前端需加长 loading、mock provider 的 `match_score` 必填约定）。

### 步骤 3 — 修正文档

3.1 删除 `NEXT_AI_TODO.md`（步骤 2 已含），并把其中仍有价值的内容合并进 `DEVELOPMENT_LOG.md`。

3.2 `docs/` 逐篇处理（我未逐篇通读，仅按关键词命中定位，执行前请先打开确认）：

| 文件 | 建议 |
|---|---|
| `docs/deployment/local.md` | **必须重写** —— 改为 `start.bat` 双击启动 / venv / npm |
| `docs/architecture/async-tasks.md` | **建议删除** —— 描述的是已移除的 Celery 架构 |
| `docs/testing/strategy.md` | **重写** —— Poetry/pnpm → venv/npm |
| `docs/design.md` | 复核后重写关键段落（架构总览是否还提 Postgres？） |
| `docs/architecture/llm.md` | 复核（mock provider 是否已写入） |
| `docs/architecture/retrieval.md` | 复核（向量检索是否还写 pgvector？现为纯 Python cosine） |
| `docs/modules/customization.md` | 复核（同步执行是否已反映） |
| `docs/modules/resume.md` | 复核（是否还写 Celery 异步解析） |
| `docs/deployment/acceptance.md` | 复核 |
| `docs/testing/test-cases.md` | 复核 |
| 其余 3 篇（未命中关键词） | 大概率可用，保存 |

### 步骤 4 — 去重与路径统一

4.1 **删除根目录 `.env.example`**，只保留 `backend/.env.example`（原因见 §4.3）。

4.2 **删除根目录 `data/`**（原因见 §4.4）。

4.3 **修正 `README.md`**：
- 环境变量一节：`copy .env.example .env` → 改为在 `backend/` 目录下执行
- 数据位置一节：`data/jd_platform.db` → `backend/data/jd_platform.db`
- 简历目录：`data/resumes/` → `backend/data/resumes/`
- 目录说明一节：删除对根 `data/` 的描述

4.4 **修正 `start.bat`**：
- 最后一行提示 `Local SQLite DB: .\data\jd_platform.db` → `.\backend\data\jd_platform.db`

4.5 **`backend/tests/conftest.py` 第 17 行**仍有 `os.environ.setdefault("REDIS_URL", ...)`，Redis 已不再使用，可删。

4.6 **`.gitignore` 补充**：加入 `*.tsbuildinfo`（`frontend/tsconfig.tsbuildinfo` 目前是构建产物却未被忽略）。

### 步骤 5 — git 固化

目前仓库只有 **2 个 commit**：

```
63a2ee4 chore: gitignore update
af72b9d initial W1-W4
```

"极简化改造"涉及约 30 个已修改文件、7 个已删除文件、6 项未跟踪新增文件，**全部未提交**。建议拆成 3 个语义化 commit：

```bash
cd "G:/期末及简历和别的项目/测试/jd-resume-platform"

# 1) 提交极简化改造本身（改 + 删）
git add -A backend/app backend/alembic backend/requirements.txt backend/alembic.ini backend/pytest.ini
git commit -m "refactor: 移除 Celery/slowapi/pgvector，切换至 SQLite + mock provider"

# 2) 提交本地启动方案与新文件
git add start.bat backend/app/db/init_db.py README.md .env.example .gitignore DEVELOPMENT_LOG.md
git commit -m "chore: 新增 start.bat 一键启动与本地化文档"

# 3) 提交本轮清理
git add -A
git commit -m "chore: 清理 Docker/Railway 残留与过时文档"
```

> 注意：步骤 1 的 `git add` 已包含 `app/tasks/` 与 `app/core/rate_limit.py` 的删除动作（`git add -A <路径>` 会记录删除）。

---

## 4. 关键结论的依据

### 4.1 为什么 `NEXT_AI_TODO.md` 是失效的

该文件第二节"还没做完的（重要）"列了 10 项，逐项核对代码现状：

| TODO 声称 | 代码实际现状 |
|---|---|
| `customizations.py` 还有 Celery 引用 | 已清除，`app/tasks/` 目录整体已删除 |
| `resumes.py` 还有 Celery 引用 | 已清除，改为同步调用 |
| `jds.py` 还有 limiter import | 已清除，`app/core/rate_limit.py` 已删 |
| 前端没改 | 确认无需改动 |
| 启动脚本没写 | `start.bat` 已存在且功能更完整 |
| README 要重写 | 已重写为本地版 |
| 测试要更新 | 需实跑确认 |

全代码库检索 `celery|slowapi|pgvector|app.tasks|rate_limit`，命中结果全部是**注释**（如 `# 同步解析 + 建索引（无 Celery）`）和 `exceptions.py` 里的错误码枚举，无任何实际引用。

**结论：该文件描述的待办已全部落地，继续保留会误导后续开发者。**

### 4.2 docs/ 关键词命中情况

`celery|docker|postgres|redis|railway|pgvector` 命中 10 篇：
`architecture/async-tasks.md`、`architecture/llm.md`、`architecture/retrieval.md`、`deployment/acceptance.md`、`deployment/local.md`、`design.md`、`modules/customization.md`、`modules/resume.md`、`testing/strategy.md`、`testing/test-cases.md`。

未命中（大概率可用）：`modules/jd.md`、`modules/llm-provider.md` 等 3 篇。

### 4.3 根 `.env.example` 与 `backend/.env.example` 该留哪一份 —— 已核实

`backend/app/core/config.py` 中：

```python
model_config = SettingsConfigDict(
    env_file=".env",          # 相对路径，相对「进程工作目录」
    env_file_encoding="utf-8",
)
```

而 `start.bat` 启动后端时先 `cd /d %~dp0\backend`，所以：

- 进程工作目录 = `backend/`
- 实际加载的配置文件 = `backend/.env`
- **根目录的 `.env.example` 永远不会被读取**

因此：**保留 `backend/.env.example`，删除根目录那份**；README 中的 `copy .env.example .env` 必须提示先 `cd backend`。

### 4.4 数据库到底在哪 —— 已核实

`.env.example` 中：`DATABASE_URL=sqlite+aiosqlite:///./data/jd_platform.db`（相对路径）

由于工作目录是 `backend/`，该路径解析为 `backend/data/jd_platform.db`。实际文件系统中：

- `backend/data/jd_platform.db` ✅ 存在，是真正的数据库
- `data/` ← 仅有 `resumes/.gitkeep`，空壳

同理 `resume_storage_dir = Path("./data/resumes")` 解析为 `backend/data/resumes/`。

因此：**删除根目录 `data/`，并把 README / start.bat 的路径文案改为 `backend/data/...`**。这比"把数据搬回根目录"改动更小，且 `.gitignore` 已经忽略了 `backend/data/`。

---

## 5. 执行清单（可勾选）

```
[ ] 1. 移出 爬面经/ 到上一级目录
[ ] 2. git rm 6 个已跟踪的过时部署文件
[ ] 3. rm 2 个未跟踪文件（DEPLOY-RAILWAY.md / NEXT_AI_TODO.md）※先誊抄有用内容
[ ] 4. 删除空的 scripts/ 目录
[ ] 5. 删除根目录 .env.example
[ ] 6. 删除根目录 data/
[ ] 7. 修正 README.md（env 路径、DB 路径、目录说明）
[ ] 8. 修正 start.bat 最后一行 DB 路径提示
[ ] 9. 删除 conftest.py 中的 REDIS_URL
[ ] 10. .gitignore 补 *.tsbuildinfo
[ ] 11. docs/deployment/local.md 重写
[ ] 12. docs/architecture/async-tasks.md 删除
[ ] 13. docs/testing/strategy.md 重写
[ ] 14. docs/ 其余 7 篇逐篇复核
[ ] 15. DEVELOPMENT_LOG.md 追加本轮记录
[ ] 16. 分 3 个 commit 提交
[ ] 17. 实跑 pytest 确认测试通过
[ ] 18. 双击 start.bat 冒烟验证
```

---

## 6. 遗留风险与提醒

1. **未提交的改造成果**风险最高 —— 建议优先执行步骤 5（git 提交），再动删除操作。
2. 步骤 2 中两个未跟踪文件删除后**不可恢复**，建议先誊抄关键信息。
3. `docs/` 我只做了关键词定位，**未逐篇通读**，判定为"复核"的 7 篇需你或后续 AI 打开确认后再决定重写或保留。
4. 报告未验证 `pytest` 当前通过情况 —— `NEXT_AI_TODO.md` 提到测试可能因旧 import 挂掉，建议执行清单第 17 项时实际跑一次。
5. 本次审计**未运行任何构建或测试**，也未修改任何文件。
